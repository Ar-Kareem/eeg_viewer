- Have advanced settings to show all channels mode where a new view appears that shows all channels in rows each channel being a different color and being very small height
- make it possible to change the default number of points
- have a full code be copyable to the clipboard which has {subject, file, channel} in a comma separated string